global class CreateCriteriaSummaryScheduled implements Schedulable
{
    global void execute(SchedulableContext SC)
    {
        /*
        CreateCriteriaSummaryWithCriteriaBatch updateBatch = new CreateCriteriaSummaryWithCriteriaBatch();
        Database.executeBatch(updateBatch, 1);
        */
    }
}